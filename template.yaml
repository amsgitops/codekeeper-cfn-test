AWSTemplateFormatVersion: '2010-09-09'
Description: CFN test fixture for CodeKeeper agent E2E
Resources:
  TestAlertsTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: codekeeper-cfn-test-alerts
      DisplayName: CodeKeeper IAM grant test 1781790279
      Tags:
        - Key: RepositoryId
          Value: amsgitops/codekeeper-cfn-test
